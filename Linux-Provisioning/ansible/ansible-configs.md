# Ansible configurations

## Install on Ubuntu WLS

1. <https://docs.ansible.com/projects/ansible/latest/installation_guide/installation_distros.html#installing-ansible-on-ubuntu>

2. Make a specific Ansible folder eg. `~/ansible`
